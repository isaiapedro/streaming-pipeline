# Local dev only — anonymous access, no TLS. Matches the NATS container's
# security posture in this MVP (see docker-compose.yml). Do not use this
# config as-is outside a local benchmark/demo environment.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
